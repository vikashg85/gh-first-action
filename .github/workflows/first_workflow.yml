name: first workflow
on: workflow_dispatch
jobs:
  first_job:
  runs-on: ubuntu-latest
  steps:
    - name: Print greeting
      run: echo "hello world"
    - name: Print Goodbye !
      run: echo "Goodbye !"
    
